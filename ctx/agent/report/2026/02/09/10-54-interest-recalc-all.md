# Отчёт итерации — interest-recalc-all

## Резюме изменений
- Пересчёт interest-score теперь выполняется для всех публикаций на странице после изменений interest vector.

## Детали работ
- В ленте заменён сбор visible-элементов на полный список загруженных публикаций и обновление маркеров для всех карточек: `web/ui/js/feed.mjs`.
- В адаптере переименован входной параметр списка публикаций, чтобы отражать полный пересчёт: `web/ui/js/interest-score.mjs`.

## Результаты
- Все карточки в DOM получают актуальные значения интереса после изменения interest vector, независимо от видимости.
