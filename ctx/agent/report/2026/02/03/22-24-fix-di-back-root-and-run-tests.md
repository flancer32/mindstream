# Отчёт итерации — fix-di-back-root-and-run-tests

## Резюме изменений
- Исправлен путь корня проекта в helper для тестового DI-контейнера.
- Прогнаны unit-тесты.

## Детали работ
- В `test/unit/di-back.mjs` скорректирован расчёт `projectRoot` до `../..`, чтобы резолвились модули из `src/`.
- Выполнен `node --test`.

## Результаты
- Все тесты прошли успешно.
